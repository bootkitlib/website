import { Route } from "@angular/router";
import { CardExampleComponent } from "./card.component";

export default [
    {
        path: '',
        component: CardExampleComponent,
    }
] satisfies Route[];
