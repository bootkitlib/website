import { Route } from "@angular/router";
import { CollapseExampleComponent } from "./collapse.component";

export default [
    {
        path: '',
        component: CollapseExampleComponent,
    }
] satisfies Route[];
