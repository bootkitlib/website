import { Route } from "@angular/router";
import { PopoverExampleComponent } from "./popover.component";

export default [
    {
        path: '',
        component: PopoverExampleComponent,
    }
] satisfies Route[];
