import { Route } from "@angular/router";
import { TooltipExampleComponent } from "./tooltip.component";

export default [
    {
        path: '',
        component: TooltipExampleComponent,
    }
] satisfies Route[];
